{
  "name": "cadmus-prosemirror-task",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:client": "pnpm --filter client dev",
    "build:prisma": "pnpm --filter server prisma:build",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
